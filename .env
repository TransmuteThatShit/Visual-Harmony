SEGMENT_LENGTH=4.0

# ─────────────────────────────────────────────────────────────────────────────
# pydub/FFmpeg
# If you have a portable FFmpeg build, point pydub to it here.
# Otherwise it will fall back to whatever “ffmpeg” is on your PATH.
FFMPEG_PATH=C:\\ffmpeg\\bin\\ffmpeg.exe

# ─────────────────────────────────────────────────────────────────────────────
# Whisper transcription model
# Options: tiny, base, small, medium, large
WHISPER_MODEL=base

# ─────────────────────────────────────────────────────────────────────────────
# Demucs stem separation
# Default “htdemucs” (new Transformer model)
# Or “mdx_extra_q” to revert to the old default
DEMUX_SUBFOLDER=htdemucs

# ─────────────────────────────────────────────────────────────────────────────
# Pitch extraction bounds (Parselmouth)
# Anything below floor or above ceiling is treated as unvoiced (NaN)
PITCH_FLOOR=75
PITCH_CEILING=1500

# ─────────────────────────────────────────────────────────────────────────────
# (Optional) Base output directory
# If you don’t want to always specify “-o output” on the CLI
OUTPUT_DIR=my_project

# ─────────────────────────────────────────────────────────────────────────────
# (Optional) External tool paths
# If you use rubberband cli from a custom install location
RUBBERBAND_PATH=C:\\Program Files\\Rubberband\\rubberband.exe
